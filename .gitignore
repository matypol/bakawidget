# Python
__pycache__/
*.pyc

# makepkg build artifacts (PKGBUILD is source, these are its output)
pkg/
src/
*.pkg.tar.*
*.log

# AUR metadata — generated by `makepkg --printsrcinfo`, not hand-edited;
# keep it out of THIS repo (it belongs in the separate AUR git repo this
# project's PKGBUILD gets copied into, per README's publishing steps)
.SRCINFO
